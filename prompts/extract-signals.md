# Warehouse expansion signal extraction

Return only JSON matching the supplied schema. Use only facts in the supplied source context. Every detected signal must cite an evidence passage. Use `null` for unknown company, industry, event, location, or date fields. Never infer that missing evidence proves an event did not happen, and never invent a citation.
